name = "Plus: save project     "
description = "(prefix+shift+s): Save this space over the project named after it."
type = "command"
command = "herdr-project save --space '{{.WorkspaceId}}' --notify --force"
